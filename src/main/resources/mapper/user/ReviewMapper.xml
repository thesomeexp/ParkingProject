<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.someexp.modules.user.mapper.ReviewMapper">
    <insert id="save" parameterType="com.someexp.modules.user.domain.entity.Review" useGeneratedKeys="true"
            keyProperty="id" keyColumn="id">
        insert into review (pid, uid, score, content, createdDate)
        values (#{pid}, #{uid}, #{score}, #{content}, now())
    </insert>
    <select id="getByPidAndUid" resultType="com.someexp.modules.user.domain.entity.Review">
        select *
        from review
        where pid = #{pid}
          and uid = #{uid}
    </select>
    <select id="listAll" resultType="com.someexp.modules.user.domain.entity.Review">
        select *
        from review
        where pid = #{pid}
        order by id desc
    </select>
</mapper>